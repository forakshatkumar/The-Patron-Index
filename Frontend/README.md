# The Patron Index Frontend

React/Vite frontend for The Patron Index.

```bash
npm install
npm run dev
```

The login screen offers **Demo Mode** for UI evaluation without a backend. For live API integration, run the backend on port 5000 or set `VITE_API_URL` in `.env`.
